{% macro listItem(props, schema = null) %}
  
  {% set schema = schema ?? craft.schema.ListItem %}
  {% set schema = schema
    .position(props.listPosition)
    .url(props.url)
  %}
  
  {% return schema %}
{% endmacro %}
