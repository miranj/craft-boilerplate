{% macro imageObject(props, schema = null) %}
  
  {% set schema = schema ?? craft.schema.ImageObject %}
  {% set schema = schema
    .url(props.url|default(null))
    .width(props.width|default(null))
    .height(props.height|default(null))
  %}
  
  {% return schema %}
{% endmacro %}
