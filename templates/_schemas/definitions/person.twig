{% macro person(props, schema = null) %}
  
  {% set schema = schema ?? craft.schema.Person %}
  {% set schema = schema
    .name(props.name)
    .url(props.url)
  %}
  
  {% return schema %}
{% endmacro %}