{#
  Teaser Router
#}

{%- extends (entry is instance of ('craft\\elements\\Category') ? [
  '_views/teasers/' ~ entry.group.handle,
] : [
  '_views/teasers/' ~ entry.section.handle ~ '-' ~ entry.type.handle,
  '_views/teasers/' ~ entry.section.handle,
  '_views/teasers/_' ~ entry.type.handle,
])|merge([
  '_views/teasers/_default',
]) -%}
