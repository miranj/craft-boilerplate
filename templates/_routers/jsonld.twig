{#
  JSON-LD Router
#}

{%- include [
  entry.section ? '_schemas/details/' ~ entry.section.handle ~ '-' ~ entry.type.handle,
  entry.section ? '_schemas/details/' ~ entry.section.handle,
  '_schemas/details/_' ~ entry.type.handle,
  '_schemas/details/_default',
]|filter -%}
