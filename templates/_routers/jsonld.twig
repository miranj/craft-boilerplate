{#
  JSON-LD Router
#}

{%- include [
  '_schemas/details/' ~ entry.section.handle ~ '-' ~ entry.type.handle,
  '_schemas/details/' ~ entry.section.handle,
  '_schemas/details/_' ~ entry.type.handle,
  '_schemas/details/' ~ ('index_' in entry.section.handle ? '_index' : 'default'),
] -%}
