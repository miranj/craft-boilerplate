{# _views/indexes/news #}
{% extends '_views/indexes/_default-filtered' %}
{% from '_helpers/filters' import yearsFilter %}



{# Filters #}
{% block response %}
  {% set indexFilters = indexFilters ?? [
    yearsFilter('year', 'Year', craft.entries({
      section: section,
    })),
  ] %}
  {{ parent() }}
{% endblock %}
