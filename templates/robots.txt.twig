{% header "Content-Type: text/plain; charset=utf-8" -%}

# robots.txt
# {{ siteUrl }}

{% for site in craft.app.sites.allSites %}
Sitemap: {{ siteUrl('sitemap.xml', siteId=site.id) }}
{% endfor %}

{# FIXME #}
{% if siteUrl|trim('/', 'right') != 'https://example.com' -%}

User-agent: *
Disallow: /

{% else -%}

User-agent: *
Disallow: /cpresources/
Disallow: /admin/

{% endif %}
